pipeline {
    agent any,
    triggers {
        pollSCM('0 * * ? * * *')
    }

    stages {
        stage('build') {
            steps {
                sh "docker -t api_test_base . Dockerfile_base"
            }
        }
    }
}